# Raíces — local Bosque evolution

Approved direction: return to Bosque, nature and roots tied to the new JR mark; authored focal motion on home, restrained continuity on interiors. Local preview only.

Focal moment: a branching vector root system grows from a central origin into three photographic worlds. Organic apertures and unequal scale replace the editing-table layout. Hover/focus traces the corresponding branch and settles the photograph; direct links remain available immediately.

Continuity: reuse a quiet root signature in the closing contact invitation throughout the site. Preserve gallery, form, service and photo-viewer behavior. Restore Bosque tokens everywhere.

Feedback: keyboard focus and pointer entry share branch selection. No pointer-following cursor, scroll hijacking or looping animation. Reduced motion presents the complete root system and removes spatial effects.

Budget: small static SVG geometry, GSAP already installed; one finite opening sequence, scoped scroll entrance on closing roots. No additional dependency or full-screen filters/canvas.

Validation: Astro check/build, existing verification/tests, desktop and narrow viewport visual inspection, direct link and focus check.

Validation completed: Astro 57 files, no diagnostics; build succeeds; verifier passes 31 generated pages and their local links/images/bilingual form taxonomy; 3 existing form transport tests pass. Browser reviewed desktop and 390px home, contact, services and product gallery; no horizontal overflow in the checked mobile routes. Fixed logo blend isolation and root SVG aspect mapping during the bounded visual pass. Reduced-motion fallback verified in source, not through an OS preference change. No external form submission or deployment.

Second authorized iteration: custom canvas root field plus desktop scroll chapters. Confirmed foreground product and portrait scenes visually, corrected fixed-stage horizontal sizing, checked 390px mobile composition (no pin and no overflow). Latest check: 58 files, zero diagnostics; build, 31-page verifier and three existing tests pass. Real-device frame-rate profiling remains unmeasured. Local only.

## Staging release authorized

User requested staging deployment after the role-specific photograph curation. Remote staging remains at 29cabbd and has no additional commits to preserve; main remains 5173927. Pre-release validation passes: Astro 58 files without diagnostics, build 31 pages, route/image/form verifier and 3 transport tests. Release includes Bosque branding, root choreography, contact refinements and selected photography/crops. Presentation PDFs in output/ are not deployment source and remain outside this release.
